import { CreditCardIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const DocumentInputIcon = (props: IconProps) => (
  <CreditCardIcon color="orange.500" {...props} />
)
